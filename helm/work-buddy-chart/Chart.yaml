# helm/work-buddy-chart/Chart.yaml
apiVersion: v2
name: work-buddy-chart
description: A Helm chart for deploying the Gen AI Work Buddy application and its microservices.
type: application # Indicates this chart is deployable
version: 0.1.0 # Initial version
appVersion: "1.0.0" # Version of the application itself

# Optional: Maintainer information
# maintainers:
# - name: YourName
#   email: your.email@example.com

# Optional: Dependencies (if we were to use sub-charts for Redis, Neo4j, etc.)
# dependencies:
# - name: redis
#   version: "15.x.x"
#   repository: "https://charts.bitnami.com/bitnami"
#   condition: redis.enabled # To conditionally deploy Redis
